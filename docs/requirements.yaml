name: docs
dependencies:
    # Base depends
  - python
  - pip

    # Package depends
  - numpy
  - matplotlib
  - pandas

    # Pip-only installs
  - pip:
    - sphinx==6.0.0
    - sphinx-autoapi
    - https://github.com/revitron/revitron-sphinx-theme/archive/master.zip
    - beautifulsoup4
    - geopy
