name: docs
channels:
dependencies:
    # Base depends
  - python
  - pip

    # Package depends
  - numpy
  - matplotlib
  - pandas
  - sphinx=6.1.3

    # Pip-only installs
  - pip:
    - sphinx-autoapi
    - https://github.com/revitron/revitron-sphinx-theme/archive/master.zip
    - beautifulsoup4
    - geopy
